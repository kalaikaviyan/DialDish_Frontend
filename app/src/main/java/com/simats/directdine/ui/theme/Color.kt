package com.simats.directdine.ui.theme

import androidx.compose.ui.graphics.Color

// --- directdine BRAND COLORS ---
// A lighter, brighter, and softer orange for Light Mode
val LightModeOrange = Color(0xFFFF8C00)
// A slightly more vibrant orange that "pops" well against dark backgrounds
val DarkModeOrange = Color(0xFFFF9800)

// --- PREMIUM SOFT DARK MODE COLORS ---
// A soft slate gray (Used by Google), absolutely NO pure black!
val SoftDarkBackground = Color(0xFF202124)
// A slightly lighter slate gray for the text boxes and cards
val SoftDarkCard = Color(0xFF303134)
val DarkModeText = Color(0xFFE8EAED)

// --- CLEAN LIGHT MODE COLORS ---
val CreamBackground = Color(0xFFFDFCFB)
val WhiteCard = Color(0xFFFFFFFF)
val LightModeText = Color(0xFF202124)